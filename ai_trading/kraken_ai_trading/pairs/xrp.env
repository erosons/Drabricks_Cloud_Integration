# XRP/USD  — ~$1.35/XRP
# Fill in credentials (same key works for all pairs)
KRAKEN_API_KEY=/QFxRsqoaS8zqfkaZ7s5PlgI2dHBygWdr0Kgb8XEcF3CcU5euUu16RoA
KRAKEN_API_SECRET=SGE5UTX7912kJ/sdIhq8bMsAo9NOfDZKuLwGRaUK74qOVH6aUz15QN5ZwTHkaTYvNvehv+WfuM5bOTDSrhHHJg==
TRADING_PAIR=XRP/USD
ORDER_SIZE=59          # 59 XRP ≈ $79.65/order
ORDER_SIZE_USD=80
MAX_POSITION=59         # 59 XRP ≈ $79.65 max held
BOOK_DEPTH=25
PRICE_DECIMALS=4       # XRP: 0.0000

IMBALANCE_THRESHOLD=0.25
MIN_CONFIDENCE=0.60
MAX_SPREAD_BPS=15.0

STOP_LOSS_USD=5.0      # $5 max loss → SL at $0.0847/XRP (6.3% drop)
RISK_REWARD_RATIO=3.0  # TP at +$15 → entry + $0.2542/XRP
TRAIL_STEP_PCT=0.25    # SL advances every $0.0636/XRP gain
PROFIT_TARGET_PCT=2.5
STOP_LOSS_PCT=0.375

DEAD_MANS_SWITCH_SECONDS=60
DRY_RUN=false
LOG_LEVEL=INFO
METRICS_PORT=8001

# Price action / trend filter
FAST_EMA=20             # short memory — tracks local momentum
SLOW_EMA=60             # long memory — defines trend direction
SPIKE_THRESHOLD=0.003   # 0.3% move from slow EMA = spike (XRP moderate volatility)
RECOVERY_PCT=0.5        # spike must recover 50% to confirm V-bottom/top
EXTREME_WINDOW=200      # look back 200 ticks for high/low context
EXHAUSTION_TTL=60
RETEST_PROXIMITY=0.002    # within 0.2% of prior V-bottom/top = re-test
RETEST_MEMORY_TICKS=400   # remember level for ~400 book ticks
RETEST_TTL=40             # signal active for 40 ticks on re-test       # ticks the reversal signal stays active after detection
