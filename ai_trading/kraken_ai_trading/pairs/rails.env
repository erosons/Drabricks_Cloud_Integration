# RAILS/USD  — ~$0.082/RAILS (Kraken symbol: RAILS/USD)
# Fill in credentials (same key works for all pairs)
KRAKEN_API_KEY=/QFxRsqoaS8zqfkaZ7s5PlgI2dHBygWdr0Kgb8XEcF3CcU5euUu16RoA
KRAKEN_API_SECRET=SGE5UTX7912kJ/sdIhq8bMsAo9NOfDZKuLwGRaUK74qOVH6aUz15QN5ZwTHkaTYvNvehv+WfuM5bOTDSrhHHJg==

TRADING_PAIR=RAILS/USD
ORDER_SIZE=976          # 976 RAILS ≈ $80.03/order
ORDER_SIZE_USD=80
MAX_POSITION=976         # 976 RAILS ≈ $80.03 max held
BOOK_DEPTH=25
PRICE_DECIMALS=4        # RAILS: 0.0000

IMBALANCE_THRESHOLD=0.25
MIN_CONFIDENCE=0.55
MAX_SPREAD_BPS=30.0     # RAILS has low liquidity — allow wider spreads

STOP_LOSS_USD=5.0      # $5 max loss → SL at $0.00512/RAILS (6.2% drop)
RISK_REWARD_RATIO=3.0  # TP at +$15 → entry + $0.0154/RAILS
TRAIL_STEP_PCT=0.25    # SL advances every $0.00384/RAILS gain
PROFIT_TARGET_PCT=2.5
STOP_LOSS_PCT=0.83

DEAD_MANS_SWITCH_SECONDS=60
DRY_RUN=false
LOG_LEVEL=INFO
METRICS_PORT=8004

# Price action / trend filter
FAST_EMA=15             # Short memory — RAILS is volatile and thinly traded
SLOW_EMA=50
SPIKE_THRESHOLD=0.008   # 0.8% move = spike (RAILS very volatile, wide tick)
RECOVERY_PCT=0.5
EXTREME_WINDOW=200
EXHAUSTION_TTL=60
RETEST_PROXIMITY=0.005    # 0.5% — wider than other pairs due to low liquidity
RETEST_MEMORY_TICKS=400
RETEST_TTL=40

# Zone scoring
MIN_ZONE_SCORE=5.0
BASING_TICKS_FAST=5
BASING_TICKS_SLOW=15
